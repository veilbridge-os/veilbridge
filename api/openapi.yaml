# GENERATED — do not edit by hand.
# Produced from the Go handler types (code-first, Huma — DESIGN D-8) via:
#   go run ./cmd/veilbridged -dump-openapi > api/openapi.yaml
# CI regenerates this and fails on drift (git diff --exit-code).
components:
  schemas:
    AddressHandout:
      additionalProperties: false
      properties:
        enabled:
          type: boolean
        first:
          type: string
        last:
          type: string
        leaseSeconds:
          format: int64
          type: integer
      required:
        - enabled
      type: object
    AddressLease:
      additionalProperties: false
      properties:
        expiresSec:
          format: int64
          type: integer
        hostname:
          type: string
        ip:
          type: string
        mac:
          type: string
      required:
        - mac
        - ip
      type: object
    ApplyConfirmInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/ApplyConfirmInputBody.json
          format: uri
          readOnly: true
          type: string
        token:
          description: Token returned by POST /apply
          type: string
      required:
        - token
      type: object
    ApplyEvent:
      additionalProperties: false
      properties:
        state:
          $ref: "#/components/schemas/ApplyState"
      required:
        - state
      type: object
    ApplyOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/ApplyOutputBody.json
          format: uri
          readOnly: true
          type: string
        detail:
          type: string
        ok:
          type: boolean
      required:
        - ok
      type: object
    ApplyState:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/ApplyState.json
          format: uri
          readOnly: true
          type: string
        deadline:
          format: date-time
          type: string
        error:
          type: string
        phase:
          type: string
        snapshot_id:
          type: string
        token:
          type: string
        window_seconds:
          format: int64
          type: integer
      required:
        - phase
        - window_seconds
      type: object
    ApplyTxInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/ApplyTxInputBody.json
          format: uri
          readOnly: true
          type: string
        timeout_seconds:
          description: Confirmation window in seconds; 0 uses the server default
          format: int64
          maximum: 600
          minimum: 0
          type: integer
      type: object
    Capability:
      additionalProperties: false
      properties:
        available:
          type: boolean
        detail:
          type: string
        reason:
          type: string
      required:
        - available
      type: object
    ChangesOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/ChangesOutputBody.json
          format: uri
          readOnly: true
          type: string
        changes:
          items:
            $ref: "#/components/schemas/ConfigChange"
          type:
            - array
            - "null"
        dangerous:
          type: boolean
      required:
        - changes
        - dangerous
      type: object
    ConfigChange:
      additionalProperties: false
      properties:
        dangerous:
          type: boolean
        detail:
          type: string
        from:
          type: string
        label:
          type: string
        labelKey:
          description: Stable key of the label, for translating it in the UI (e.g. network.uplink.proto). The set of keys is fixed by the device; empty when there are no words for this setting yet and label is all there is.
          type: string
        subject:
          description: "Which entry the row is about when the setting belongs to one of several (a rule, a port forward, a reserved address): its name, or a description of it. Empty for a setting that exists once, and for a row that adds or removes a whole entry, whose value already describes it."
          type: string
        to:
          type: string
      required:
        - label
        - from
        - to
        - dangerous
      type: object
    DiagnosticsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/DiagnosticsOutputBody.json
          format: uri
          readOnly: true
          type: string
        output:
          type: string
        target:
          type: string
      required:
        - target
        - output
      type: object
    ErrorDetail:
      additionalProperties: false
      properties:
        location:
          description: Where the error occurred, e.g. 'body.items[3].tags' or 'path.thing-id'
          type: string
        message:
          description: Error message text
          type: string
        value:
          description: The value at the given location
      type: object
    ErrorEvent:
      additionalProperties: false
      properties:
        detail:
          type: string
        topic:
          type: string
      required:
        - topic
        - detail
      type: object
    ErrorModel:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/ErrorModel.json
          format: uri
          readOnly: true
          type: string
        detail:
          description: A human-readable explanation specific to this occurrence of the problem.
          examples:
            - Property foo is required but is missing.
          type: string
        errors:
          description: Optional list of individual error details
          items:
            $ref: "#/components/schemas/ErrorDetail"
          type:
            - array
            - "null"
        instance:
          description: A URI reference that identifies the specific occurrence of the problem.
          examples:
            - https://example.com/error-log/abc123
          format: uri
          type: string
        status:
          description: HTTP status code
          examples:
            - 400
          format: int64
          type: integer
        title:
          description: A short, human-readable summary of the problem type. This value should not change between occurrences of the error.
          examples:
            - Bad Request
          type: string
        type:
          default: about:blank
          description: A URI reference to human-readable documentation for the error.
          examples:
            - https://example.com/errors/example
          format: uri
          type: string
      type: object
    FirewallRule:
      additionalProperties: false
      properties:
        action:
          type: string
        enabled:
          type: boolean
        family:
          type: string
        from:
          type: string
        id:
          type: string
        name:
          type: string
        ports:
          type: string
        protocols:
          items:
            type: string
          type:
            - array
            - "null"
        system:
          type: boolean
        to:
          type: string
        unsupported:
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - id
        - enabled
        - system
        - protocols
        - action
        - unsupported
      type: object
    FirewallRuleConfig:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/FirewallRuleConfig.json
          format: uri
          readOnly: true
          type: string
        action:
          description: accept, reject (the sender is told) or drop (silently)
          type: string
        before:
          description: "For a new rule: the rule (id from GET /firewall) to place it in front of; empty puts it last. The first matching rule wins."
          type: string
        enabled:
          type: boolean
        family:
          description: ipv4 or ipv6 to limit the rule to one; empty for both
          type: string
        from:
          description: Zone the traffic comes from, as reported by GET /firewall, or * for any
          type: string
        id:
          description: Rule to edit, as reported by GET /firewall; empty adds a new one
          type: string
        name:
          maxLength: 64
          type: string
        ports:
          description: "Destination ports for TCP/UDP: one, first-last, or several separated by spaces or commas; empty for any"
          type: string
        protocols:
          items:
            type: string
          maxItems: 3
          minItems: 1
          type:
            - array
            - "null"
        to:
          description: Zone the traffic goes to; empty for the router itself, * for any zone
          type: string
      required:
        - enabled
        - from
        - protocols
        - action
      type: object
    FirewallStatus:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/FirewallStatus.json
          format: uri
          readOnly: true
          type: string
        forwardings:
          items:
            $ref: "#/components/schemas/ZoneForwarding"
          type:
            - array
            - "null"
        portForwards:
          items:
            $ref: "#/components/schemas/PortForward"
          type:
            - array
            - "null"
        rules:
          items:
            $ref: "#/components/schemas/FirewallRule"
          type:
            - array
            - "null"
        zones:
          items:
            $ref: "#/components/schemas/FirewallZone"
          type:
            - array
            - "null"
      required:
        - zones
        - forwardings
        - portForwards
        - rules
      type: object
    FirewallZone:
      additionalProperties: false
      properties:
        forward:
          type: string
        input:
          type: string
        live:
          type: boolean
        masquerade:
          type: boolean
        name:
          type: string
        networks:
          items:
            type: string
          type:
            - array
            - "null"
        output:
          type: string
        role:
          type: string
      required:
        - name
        - networks
        - live
        - input
        - output
        - forward
        - masquerade
      type: object
    HandoutConfig:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/HandoutConfig.json
          format: uri
          readOnly: true
          type: string
        enabled:
          type: boolean
        first:
          type: string
        last:
          type: string
        leaseSeconds:
          format: int64
          type: integer
      required:
        - enabled
      type: object
    ImportInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/ImportInputBody.json
          format: uri
          readOnly: true
          type: string
        content:
          description: Raw .conf content (kind=awg-config)
          type: string
        kind:
          description: Import source kind
          enum:
            - awg-config
            - subscription
          type: string
        url:
          description: Subscription URL (kind=subscription)
          type: string
      required:
        - kind
      type: object
    LANConfig:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/LANConfig.json
          format: uri
          readOnly: true
          type: string
        address:
          type: string
        netmask:
          type: string
      required:
        - address
        - netmask
      type: object
    LANStatus:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/LANStatus.json
          format: uri
          readOnly: true
          type: string
        handout:
          $ref: "#/components/schemas/AddressHandout"
        interface:
          $ref: "#/components/schemas/NetworkInterface"
        leases:
          items:
            $ref: "#/components/schemas/AddressLease"
          type:
            - array
            - "null"
        reserved:
          items:
            $ref: "#/components/schemas/ReservedAddress"
          type:
            - array
            - "null"
      required:
        - interface
        - handout
      type: object
    LoginInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/LoginInputBody.json
          format: uri
          readOnly: true
          type: string
        password:
          description: Admin password
          type: string
      required:
        - password
      type: object
    LoginOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/LoginOutputBody.json
          format: uri
          readOnly: true
          type: string
        expiresAt:
          description: RFC3339 expiry
          type: string
        token:
          description: JWT bearer token
          type: string
      required:
        - token
        - expiresAt
      type: object
    MetricsOutputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/MetricsOutputBody.json
          format: uri
          readOnly: true
          type: string
        bufferBytes:
          format: int64
          type: integer
        day:
          items:
            $ref: "#/components/schemas/Sample"
          type:
            - array
            - "null"
        live:
          items:
            $ref: "#/components/schemas/Sample"
          type:
            - array
            - "null"
      required:
        - live
        - day
        - bufferBytes
      type: object
    MoveFirewallRuleInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/MoveFirewallRuleInputBody.json
          format: uri
          readOnly: true
          type: string
        before:
          description: Rule (id from GET /firewall) to place it in front of; empty moves it to the end. The first matching rule wins.
          type: string
      type: object
    NetworkInterface:
      additionalProperties: false
      properties:
        device:
          type: string
        dns:
          items:
            type: string
          type:
            - array
            - "null"
        gateway:
          type: string
        gateway6:
          type: string
        ipv4:
          items:
            type: string
          type:
            - array
            - "null"
        ipv6:
          items:
            type: string
          type:
            - array
            - "null"
        name:
          type: string
        proto:
          type: string
        up:
          type: boolean
        uptimeSec:
          format: int64
          type: integer
      required:
        - name
        - up
      type: object
    Node:
      additionalProperties: false
      properties:
        endpoint:
          type: string
        engine:
          type: string
        id:
          type: string
        name:
          type: string
      required:
        - id
        - name
        - engine
        - endpoint
      type: object
    NodeStatus:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/NodeStatus.json
          format: uri
          readOnly: true
          type: string
        active:
          type: boolean
        handshakeAgeSec:
          format: int64
          type: integer
        lastError:
          type: string
        nodeId:
          type: string
        rxBytes:
          format: int64
          type: integer
        txBytes:
          format: int64
          type: integer
      required:
        - nodeId
        - active
        - handshakeAgeSec
        - rxBytes
        - txBytes
      type: object
    NodeWithStatus:
      additionalProperties: false
      properties:
        node:
          $ref: "#/components/schemas/Node"
        status:
          $ref: "#/components/schemas/NodeStatus"
      required:
        - node
      type: object
    PathProbe:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/PathProbe.json
          format: uri
          readOnly: true
          type: string
        actualVia:
          type: string
        detail:
          type: string
        expectedVia:
          type: string
        ok:
          type: boolean
        target:
          type: string
      required:
        - target
        - expectedVia
        - actualVia
        - ok
      type: object
    PortForward:
      additionalProperties: false
      properties:
        enabled:
          type: boolean
        externalPort:
          type: string
        from:
          type: string
        id:
          type: string
        name:
          type: string
        protocols:
          items:
            type: string
          type:
            - array
            - "null"
        toAddress:
          type: string
        toPort:
          type: string
      required:
        - id
        - enabled
        - protocols
        - from
        - externalPort
        - toAddress
        - toPort
      type: object
    PortForwardConfig:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/PortForwardConfig.json
          format: uri
          readOnly: true
          type: string
        enabled:
          type: boolean
        externalPort:
          description: Port or first-last range on the router
          type: string
        id:
          description: Entry to edit, as reported by GET /firewall; empty adds a new one
          type: string
        name:
          maxLength: 64
          type: string
        protocols:
          items:
            type: string
          maxItems: 2
          minItems: 1
          type:
            - array
            - "null"
        toAddress:
          description: Device on the local network the connections go to
          type: string
        toPort:
          description: Port on that device; empty keeps the external one
          type: string
      required:
        - enabled
        - protocols
        - externalPort
        - toAddress
      type: object
    ProbeInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/ProbeInputBody.json
          format: uri
          readOnly: true
          type: string
        expectedVia:
          description: Expected path
          enum:
            - tunnel
            - direct
          type: string
        target:
          description: Domain or IP to test
          type: string
      required:
        - target
        - expectedVia
      type: object
    ReservationConfig:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/ReservationConfig.json
          format: uri
          readOnly: true
          type: string
        ip:
          type: string
        mac:
          type: string
        name:
          type: string
      required:
        - mac
        - ip
      type: object
    ReservedAddress:
      additionalProperties: false
      properties:
        id:
          type: string
        ip:
          type: string
        mac:
          type: string
        name:
          type: string
      required:
        - mac
        - ip
      type: object
    RouteInterface:
      additionalProperties: false
      properties:
        ipv4:
          items:
            type: string
          type:
            - array
            - "null"
        name:
          type: string
        up:
          type: boolean
      required:
        - name
        - up
        - ipv4
      type: object
    RouteRule:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/RouteRule.json
          format: uri
          readOnly: true
          type: string
        id:
          type: string
        kind:
          type: string
        note:
          type: string
        target:
          type: string
        value:
          type: string
      required:
        - id
        - kind
        - value
        - target
      type: object
    RoutesStatus:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/RoutesStatus.json
          format: uri
          readOnly: true
          type: string
        interfaces:
          items:
            $ref: "#/components/schemas/RouteInterface"
          type:
            - array
            - "null"
        routes:
          items:
            $ref: "#/components/schemas/StaticRoute"
          type:
            - array
            - "null"
      required:
        - routes
        - interfaces
      type: object
    RuleBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/RuleBody.json
          format: uri
          readOnly: true
          type: string
        kind:
          description: Match kind
          enum:
            - domain
            - subnet
          type: string
        note:
          description: User comment
          type: string
        target:
          description: Where to send it
          enum:
            - tunnel
            - direct
          type: string
        value:
          description: Domain or CIDR
          type: string
      required:
        - kind
        - value
        - target
      type: object
    Sample:
      additionalProperties: false
      properties:
        at:
          format: int64
          type: integer
        cpuPercent:
          format: double
          type: number
        memTotal:
          format: int64
          type: integer
        memUsed:
          format: int64
          type: integer
        storageUsed:
          format: int64
          type: integer
      required:
        - at
        - cpuPercent
        - memUsed
        - memTotal
        - storageUsed
      type: object
    StaticRoute:
      additionalProperties: false
      properties:
        active:
          type: boolean
        enabled:
          type: boolean
        family:
          type: string
        gateway:
          type: string
        id:
          type: string
        interface:
          type: string
        metric:
          format: int64
          type: integer
        name:
          type: string
        target:
          type: string
        unsupported:
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - id
        - enabled
        - family
        - target
        - interface
        - metric
        - active
        - unsupported
      type: object
    StaticRouteConfig:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/StaticRouteConfig.json
          format: uri
          readOnly: true
          type: string
        enabled:
          type: boolean
        gateway:
          description: Next hop on the chosen connection's network; empty for a network reached directly
          type: string
        id:
          description: Route to edit, as reported by GET /network/routes; empty adds a new one
          type: string
        interface:
          description: Connection to leave through (GET /network/routes lists them); empty picks the one whose network holds the gateway
          type: string
        metric:
          description: Lower wins between routes to the same network
          format: int64
          maximum: 65535
          minimum: 0
          type: integer
        name:
          maxLength: 64
          type: string
        target:
          description: IPv4 network (10.8.0.0/24) or a single address; host bits are cleared the way the device clears them
          type: string
      required:
        - enabled
        - target
      type: object
    SystemEvent:
      additionalProperties: false
      properties:
        sample:
          $ref: "#/components/schemas/Sample"
        system:
          $ref: "#/components/schemas/SystemInfo"
      required:
        - system
        - sample
      type: object
    SystemInfo:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/SystemInfo.json
          format: uri
          readOnly: true
          type: string
        cpuPercent:
          format: double
          type: number
        egressGeo:
          type: string
        egressIP:
          type: string
        firmware:
          type: string
        hostname:
          type: string
        kernel:
          type: string
        loadAvg:
          items:
            format: double
            type: number
          maxItems: 3
          minItems: 3
          type:
            - array
            - "null"
        memTotal:
          format: int64
          type: integer
        memUsed:
          format: int64
          type: integer
        model:
          type: string
        platform:
          type: string
        storageTotal:
          format: int64
          type: integer
        storageUsed:
          format: int64
          type: integer
        tunnelEngine:
          type: string
        tunnelUp:
          type: boolean
        uptimeSec:
          format: int64
          type: integer
        wanIP:
          type: string
      required:
        - platform
        - hostname
        - uptimeSec
        - cpuPercent
        - loadAvg
        - memUsed
        - memTotal
        - tunnelUp
      type: object
    WANConfig:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/WANConfig.json
          format: uri
          readOnly: true
          type: string
        address:
          type: string
        dns:
          items:
            type: string
          type:
            - array
            - "null"
        gateway:
          type: string
        interface:
          type: string
        netmask:
          type: string
        password:
          type: string
        proto:
          type: string
        username:
          type: string
      required:
        - proto
      type: object
    WANStatus:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - /api/v1/schemas/WANStatus.json
          format: uri
          readOnly: true
          type: string
        candidates:
          items:
            type: string
          type:
            - array
            - "null"
        interface:
          $ref: "#/components/schemas/NetworkInterface"
        selectedBy:
          type: string
      required:
        - interface
        - selectedBy
      type: object
    ZoneForwarding:
      additionalProperties: false
      properties:
        from:
          type: string
        to:
          type: string
      required:
        - from
        - to
      type: object
  securitySchemes:
    bearerAuth:
      bearerFormat: JWT
      scheme: bearer
      type: http
info:
  title: VeilBridge Router Core API
  version: 0.1.0
openapi: 3.1.0
paths:
  /apply:
    get:
      operationId: applyState
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyState"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: State of the apply transaction
      tags:
        - apply
    post:
      operationId: applyConfig
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplyTxInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyState"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Apply staged configuration with an automatic revert
      tags:
        - apply
  /apply/changes:
    delete:
      operationId: discardStaged
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Throw away the staged draft without touching the device
      tags:
        - apply
    get:
      operationId: stagedChanges
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Configuration edits staged but not yet applied
      tags:
        - apply
  /apply/confirm:
    post:
      operationId: confirmApply
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplyConfirmInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyState"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Confirm that the panel survived the change
      tags:
        - apply
  /apply/revert:
    post:
      operationId: revertApply
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyState"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Undo the pending change immediately
      tags:
        - apply
  /auth/login:
    post:
      operationId: login
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security: []
      summary: Exchange admin password for a JWT
      tags:
        - auth
  /capabilities:
    get:
      operationId: getCapabilities
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  $ref: "#/components/schemas/Capability"
                type: object
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: What this device can do, and why not when it cannot
      tags:
        - system
  /config/export:
    get:
      operationId: exportConfig
      responses:
        "200":
          content:
            application/json:
              schema:
                description: Full VeilBridge config document (includes secrets)
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Export the full config (includes secrets)
      tags:
        - config
  /config/import:
    post:
      operationId: importConfig
      requestBody:
        content:
          application/octet-stream:
            schema:
              contentMediaType: application/octet-stream
              format: binary
              type: string
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Import and apply a backup
      tags:
        - config
  /events:
    get:
      operationId: events
      responses:
        "200":
          content:
            text/event-stream:
              schema:
                description: Each oneOf object in the array represents one possible Server Sent Events (SSE) message, serialized as UTF-8 text according to the SSE specification.
                items:
                  oneOf:
                    - properties:
                        data:
                          $ref: "#/components/schemas/ApplyEvent"
                        event:
                          const: apply
                          description: The event name.
                          type: string
                        id:
                          description: The event ID.
                          type: integer
                        retry:
                          description: The retry time in milliseconds.
                          type: integer
                      required:
                        - data
                        - event
                      title: Event apply
                      type: object
                    - properties:
                        data:
                          $ref: "#/components/schemas/ErrorEvent"
                        event:
                          const: error
                          description: The event name.
                          type: string
                        id:
                          description: The event ID.
                          type: integer
                        retry:
                          description: The retry time in milliseconds.
                          type: integer
                      required:
                        - data
                        - event
                      title: Event error
                      type: object
                    - properties:
                        data:
                          $ref: "#/components/schemas/SystemEvent"
                        event:
                          const: system
                          description: The event name.
                          type: string
                        id:
                          description: The event ID.
                          type: integer
                        retry:
                          description: The retry time in milliseconds.
                          type: integer
                      required:
                        - data
                        - event
                      title: Event system
                      type: object
                title: Server Sent Events
                type: array
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: "Live updates: one stream, topics system/apply/error"
      tags:
        - system
  /firewall:
    get:
      operationId: getFirewall
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FirewallStatus"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Zones, what they allow, forwarded ports and traffic rules
      tags:
        - firewall
  /firewall/port-forwards:
    put:
      operationId: stagePortForward
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PortForwardConfig"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Stage a port forward, new or edited (does not apply it)
      tags:
        - firewall
  /firewall/port-forwards/{id}:
    delete:
      operationId: removePortForward
      parameters:
        - description: Port forward entry as reported by GET /firewall
          in: path
          name: id
          required: true
          schema:
            description: Port forward entry as reported by GET /firewall
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Stage the removal of a port forward (does not apply it)
      tags:
        - firewall
  /firewall/rules:
    put:
      operationId: stageFirewallRule
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FirewallRuleConfig"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Stage a traffic rule of the owner's, new or edited (does not apply it)
      tags:
        - firewall
  /firewall/rules/{id}:
    delete:
      operationId: removeFirewallRule
      parameters:
        - description: Rule as reported by GET /firewall
          in: path
          name: id
          required: true
          schema:
            description: Rule as reported by GET /firewall
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Stage the removal of a traffic rule of the owner's (does not apply it)
      tags:
        - firewall
  /firewall/rules/{id}/move:
    post:
      operationId: moveFirewallRule
      parameters:
        - description: Rule to move, as reported by GET /firewall
          in: path
          name: id
          required: true
          schema:
            description: Rule to move, as reported by GET /firewall
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MoveFirewallRuleInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Stage moving a traffic rule of the owner's in the list (does not apply it)
      tags:
        - firewall
  /network/interfaces:
    get:
      operationId: listInterfaces
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/NetworkInterface"
                type:
                  - array
                  - "null"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: L3 interfaces as the router's network daemon sees them
      tags:
        - network
  /network/lan:
    get:
      operationId: getLAN
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LANStatus"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: The local network, its address handout and its clients
      tags:
        - network
    put:
      operationId: stageLAN
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LANConfig"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Stage this router's own address on the local network (does not apply it)
      tags:
        - network
  /network/lan/handout:
    put:
      operationId: stageHandout
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HandoutConfig"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Stage the address handout (does not apply it)
      tags:
        - network
  /network/lan/reservations:
    put:
      operationId: stageReservation
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReservationConfig"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Stage an address reserved for one device (does not apply it)
      tags:
        - network
  /network/lan/reservations/{id}:
    delete:
      operationId: removeReservation
      parameters:
        - description: Reservation entry as reported by GET /network/lan
          in: path
          name: id
          required: true
          schema:
            description: Reservation entry as reported by GET /network/lan
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Stage the removal of a reserved address (does not apply it)
      tags:
        - network
  /network/routes:
    get:
      operationId: getStaticRoutes
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoutesStatus"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Static routes, each with whether the kernel is using it, and the connections a route can use
      tags:
        - network
    put:
      operationId: stageStaticRoute
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaticRouteConfig"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Stage an IPv4 static route, new or edited (does not apply it)
      tags:
        - network
  /network/routes/{id}:
    delete:
      operationId: removeStaticRoute
      parameters:
        - description: Route as reported by GET /network/routes
          in: path
          name: id
          required: true
          schema:
            description: Route as reported by GET /network/routes
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Stage the removal of a static route (does not apply it)
      tags:
        - network
  /network/wan:
    get:
      operationId: getWAN
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WANStatus"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: The uplink, and which rule identified it
      tags:
        - network
    put:
      operationId: stageWAN
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WANConfig"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChangesOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Stage a new uplink configuration (does not apply it)
      tags:
        - network
  /nodes:
    get:
      operationId: listNodes
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/NodeWithStatus"
                type:
                  - array
                  - "null"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: List nodes with status
      tags:
        - nodes
  /nodes/import:
    post:
      operationId: importNodes
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImportInputBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/Node"
                type:
                  - array
                  - "null"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Import node(s) from a .conf or subscription
      tags:
        - nodes
  /nodes/{id}:
    delete:
      operationId: removeNode
      parameters:
        - description: Node ID
          in: path
          name: id
          required: true
          schema:
            description: Node ID
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Remove a node
      tags:
        - nodes
  /nodes/{id}/activate:
    post:
      operationId: activateNode
      parameters:
        - description: Node ID
          in: path
          name: id
          required: true
          schema:
            description: Node ID
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SystemInfo"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Switch the active exit to this node
      tags:
        - nodes
  /nodes/{id}/status:
    get:
      operationId: nodeStatus
      parameters:
        - description: Node ID
          in: path
          name: id
          required: true
          schema:
            description: Node ID
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NodeStatus"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Fresh status for one node
      tags:
        - nodes
  /routes:
    get:
      operationId: listRoutes
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/RouteRule"
                type:
                  - array
                  - "null"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: List routing rules
      tags:
        - routes
    post:
      operationId: addRoute
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RuleBody"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RouteRule"
          description: Created
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Add a routing rule
      tags:
        - routes
  /routes/apply:
    post:
      operationId: applyRoutes
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Apply the rule set to the OS
      tags:
        - routes
  /routes/{id}:
    delete:
      operationId: deleteRoute
      parameters:
        - description: Node ID
          in: path
          name: id
          required: true
          schema:
            description: Node ID
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Delete a routing rule
      tags:
        - routes
    put:
      operationId: updateRoute
      parameters:
        - description: Rule ID
          in: path
          name: id
          required: true
          schema:
            description: Rule ID
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RuleBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RouteRule"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Update a routing rule
      tags:
        - routes
  /system:
    get:
      operationId: getSystem
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SystemInfo"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: System snapshot for the dashboard
      tags:
        - system
  /system/diagnostics:
    get:
      operationId: diagnostics
      parameters:
        - description: Host or IP to diagnose
          explode: false
          in: query
          name: target
          schema:
            description: Host or IP to diagnose
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiagnosticsOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: ping/traceroute from the agent
      tags:
        - system
  /system/metrics:
    get:
      operationId: getMetrics
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricsOutputBody"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: CPU/memory/storage history kept in RAM
      tags:
        - system
  /system/probe:
    post:
      operationId: probePath
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProbeInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PathProbe"
          description: OK
        default:
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorModel"
          description: Error
      security:
        - bearerAuth: []
      summary: Probe whether traffic goes via tunnel or direct
      tags:
        - system
servers:
  - url: /api/v1
