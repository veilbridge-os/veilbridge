# Release notes

One file per tag, named exactly like the tag (`v0.2.0-alpha1.md`). The release
workflow publishes this file as the release description and refuses to publish
without it; the generated list of commits is appended below. What goes in a
note and how releases are cut: [`RELEASING.md`](../../RELEASING.md).
